<form>
  {{input type="text" required="true" autofocus="true" placeholder="Comment" value=comment}}
  <button {{action 'addComment'}}>Post</button>
  {{#if error}}<div>Error: {{error}}.</div>{{/if}}
  <br>
  {{comment}}
</form>

